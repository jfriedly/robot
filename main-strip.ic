#use "defines.ic"
#use "line.ic"
#use "util.ic"


int main()
{
    for(;;) {
        printf("press start\n");
        while(!start_button());
        
        while(!stop_button());
        follow_strip_back();
    }
    
}
