#use "defines.ic"

void main() {
    for(;;) {
        printf("WL:%d WR:%d BL:%d  Elec:%d\n", digital(SWITCH_WALL_LEFT), digital(SWITCH_WALL_RIGHT), digital(SWITCH_CORN), analog(29));
        msleep(250L);
    }
}
